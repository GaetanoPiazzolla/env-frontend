export const environment = {
  production: true, // this could be "environmented" also :D
  apiBackendUrl: window['env'].API_BACKEND_URL,
  externalIntegrationUrl: window['env'].EXTERNAL_INTEGRATION_URL
};
